#!/usr/bin/expect -f


# Run airodump-ng and wait for it to start
spawn bash -c "airodump-ng wlan1 | tee signalpipe monitor.txt > /dev/null"
expect "airodump-ng"


# Send simulated keypresses (e.g., 'q' twice)
send "a"
sleep 1
send "a"
sleep 1
send "a"

set pipe [open "signalpipe" r]



interact


