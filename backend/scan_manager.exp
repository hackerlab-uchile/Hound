#!/usr/bin/expect -f

  

# Run airodump-ng and wait for it to start
spawn airodump-ng wlan1 2>&1 > fifo1 
expect "airodump-ng"
sleep 1

# Send simulated keypresses (e.g., 'q' twice)
send "a"
sleep 1
send "a"
sleep 1
send "a"


interact


